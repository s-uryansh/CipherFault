# Payment Service Demo

Tiny service-shaped C program for showing the intended workflow:

```bash
make
../../.venv/bin/python -m cipherfault.cli scan ./payment-service.out
```

CipherFault scans the compiled ELF binary, not the C source or the folder.
